services:

  redis:
    image: redis:latest
    command: ["redis-server", "--save", "", "--loglevel", "warning"]
    ports:
      - "6379:6379"
    networks:
      - container_network

  backend: # Django
    build: .
    ports:
      - "8000:8000"
    networks:
      - container_network
    extra_hosts:
      - "host.docker.internal:host-gateway"

networks:
  container_network:
    driver: bridge
